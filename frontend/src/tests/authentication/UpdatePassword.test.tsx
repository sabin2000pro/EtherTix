import React from 'react';
import { render, fireEvent } from "@testing-library/react";
import UpdatePassword from 'pages/auth/UpdatePassword';

describe("Update Password Test Suite", () => {

    it("Should render the component with empty fields", () => { // Unit Test 1

    })

    it("Show confirmation message if the passwords do not match", () => { // Unit Test 2

    })

    it("should call update password function with correct data when form is submitted", () => {
       
    })

})