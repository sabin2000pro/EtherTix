export interface IBooking {
    bookings: any[],
    loading?: boolean,
    message?: string,
    error?: string
}

export interface ISingleBooking {
    booking: {},
    loading?: boolean,
    message?: string,
    error?: string 
}
